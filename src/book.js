function createTitle(title) {
  var title = title;
  console.log (`The ${title}`);
}

function buildMainCharacter() {

}






module.exports = {
  createTitle
  buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
};
